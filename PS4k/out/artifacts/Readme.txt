- "publish.bat" contains absolute filepath!

- To add new Jars:
Add them to the lib folder (if new subdir add that to dependencies, for intellij compilation).
Add to the artifact build (jar, so they are exported)
Add to classpath (project settings)
Add to GetDown getdown.txt