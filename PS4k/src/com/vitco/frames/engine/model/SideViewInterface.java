package com.vitco.frames.engine.model;

import javax.swing.*;

/**
 * Can render an AnimationData and provides means to alter the data.
 */
public interface SideViewInterface {
    JPanel buildSides(JPanel pane);
}
