package com.vitco.engine.adapter;

import com.vitco.engine.view.DrawContainer;

/**
 * Manages the mouse events for animation interaction depending on the currently selected tool.
 */
public class AnimationAdapter extends AbstractAdapter {

    // constructor
    public AnimationAdapter(DrawContainer container) {
        super(container);
    }
}
