package com.vitco.engine.adapter;

import com.vitco.engine.view.DrawContainer;

/**
 * Manages the mouse events for voxel interaction depending on the currently selected tool.
 */
public class VoxelAdapter extends AbstractAdapter {

    // constructor
    public VoxelAdapter(DrawContainer container) {
        super(container);
    }

}
